{
  "schemaVersion": 1,
  "id": "${MODID}",
  "name": "${NAME}",
  "version": "${version}",
  "environment": "*",
  "icon": "assets/${MODID}/icon.png",
  "authors": ["${AUTHOR}"],
  "license": "BSD-2-Clause",
  "contact": {
    "homepage": "${CURSE_HOMEPAGE}",
    "sources": "${GIT_REPO}",
    "issues": "${GIT_REPO}/issues"
  },
  "license": "${LICENSE}",
  "icon": "assets/${MODID}/icon.png",
  "environment": "*",
  "entrypoints": {
    "main": [
      "at.alex.timechanger.Timechanger"
    ],
    "client": [
      "at.alex.timechanger.TimeChangerClient"
    ],
    "modmenu": [
      "at.alex.timechanger.ModMenuImpl"
    ]
  },
  "mixins": ["${MODID}.mixins.json"],
  "depends": {
    "fabricloader": ">=${FABRIC_LOADER}",
    "java": ">=${JAVA}",
    "minecraft": "${MINECRAFT}"
  },
  "suggests": {
    "another-mod": "*"
  }
}
